每个镜像都有一个名字和tag

dockerfile里描述了分层结构

可以通过docker run命令，指定这个镜像来运行某些程序

在我们启动了一个容器并执行bash后，我们可以在其中安装各种依赖，把自己的应用代码拷进去并执行。如果要保存这些更改，应该使用Dockerfile。

使用COPY指令把我们的代码文件拷贝到镜像内部
然后使用WORKDIR指令，指定运行时的工作路径
FROM后面是镜像名
